export class Profile {
    constructor( {id, full_name, role, balance, carnet} ) {
        this.id = id;
        this.full_name = full_name;
        this.role = role;
        this.balance = balance;
        this.carnet = carnet;
    }
}